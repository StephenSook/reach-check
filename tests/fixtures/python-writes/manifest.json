{
  "name": "python-writes",
  "description": "A Python body that writes to disk. Almost every Play is python-bodied, so missing these reported zero writes for packages that clearly write.",
  "metadata": {"runtime_dependencies": {"host_tools": [{"id": "python3", "command": "python3", "required": true}]}},
  "steps": {"probe": {"type": "process.exec", "argv": ["python3", "@resource{writer.py}"]}}
}
